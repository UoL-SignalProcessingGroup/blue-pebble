version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.12"
  jobs:
    post_install:
      - python -m pip install --upgrade pip

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - examples
